language: rust
rust:
  - stable
  - beta
  - nightly
cache: cargo
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
before_install:
  - sudo apt-get install -y llvm-dev
script:
  - cargo install --path .
  - bash test/test_llvm_passes.sh
